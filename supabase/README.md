# Supabase

- `migrations/` — SQL versionado (Fase 1 del BLUEPRINT.md, sección 05).
- Requiere la extensión `unaccent` habilitada para la deduplicación por nombre (sección 13).
- `seed.sql` (se agrega en la Fase 1) carga los datos de la sucursal inicial (`stores`) y, opcionalmente, el catálogo legado en `docs/legacy-reference/catalog/`.
